[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

# Disable Docker security checks that fail on Railway's env var handling
[phases.setup]
aptPkgs = []

# Railway handles secrets at runtime, not build time
# This disables the strict Dockerfile linting
